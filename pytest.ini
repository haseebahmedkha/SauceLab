[pytest]
testpaths = tests
addopts = -v -s

markers =
    smoke: smoke tests (build verification)
    sanity: sanity tests (critical flows)
    regression: regression tests (full coverage)
    e2e: end to end tests
    critical: critical business tests
